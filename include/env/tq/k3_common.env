
/* SPDX-License-Identifier: GPL-2.0-or-later */
/*
 * Copyright (c) 2022-2025 TQ-Systems GmbH <u-boot@ew.tq-group.com>, D-82229 Seefeld, Germany.
 * Author: Nora Schiffer
 */

#include <env/tq/k3_update.env>

/*
 * Addresses above 0x88000000 (loadaddr) are used to avoid conflicts with
 * load addresses specified in FIT images
 */
kernel_addr_r=CONFIG_SYS_LOAD_ADDR
scriptaddr=0x8a800000
pxefile_addr_r=0x8ac00000
fdt_addr_r=0x8b000000
fdtoverlay_addr_r=0x8b800000
initrd_addr_r=0x8c000000

/* Offset for spi_flash bootdev */
script_offset_f=0x7f0000
script_size_f=0x10000

/* Partition names for update_uboot_spi_flash and boot scripts */
tiboot3_part_f=ospi.tiboot3
uboot_part_f=ospi.u-boot
script_part_f=ospi.script
rootfs_part_f=ospi.rootfs

update_uboot_mmc0=devnum=0; run do_update_uboot_mmc
update_uboot_mmc1=devnum=1; run do_update_uboot_mmc
update_uboot_spi_flash0=busnum=0; run do_update_uboot_spi_flash
update_bootscript_mmc0=devnum=0; run do_update_bootscript_mmc
update_bootscript_mmc1=devnum=1; run do_update_bootscript_mmc
update_bootscript_spi_flash0=busnum=0; run do_update_bootscript_spi_flash
